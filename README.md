# projet_syslog
This is a syslog project of an enterprise

# Deployment
The entire infra. is deployed in docker. to build the docker images, deploy the infra run the makefile
```shell
make
build                            build docker images
network                          create network
run                              run infrastructure
```

